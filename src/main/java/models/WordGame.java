package models;
import java.util.List;
import java.util.ArrayList;

public class WordGame {

    public String replaceWord(String originalString, String oldWord, String newWord){
        return originalString.replace(oldWord, newWord);
    }
}
